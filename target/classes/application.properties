# =====================================================
#  application.properties
#  Change the values below to match your PostgreSQL setup
# =====================================================

# --- Database ---
spring.datasource.url=jdbc:postgresql://localhost:5432/questionpaperdb
spring.datasource.username=postgres
spring.datasource.password=Bala@2007
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Connection Pool ---
spring.datasource.hikari.maximum-pool-size=5

# --- Server ---
server.port=8080

# --- Static files (HTML, CSS, JS served from /resources/static) ---
spring.web.resources.static-locations=classpath:/static/
